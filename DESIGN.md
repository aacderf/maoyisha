# 茂一杀个人网站设计约束

## 视觉方向

站点是游戏作品官网，不是普通作品集模板。深色是默认主视觉，浅色作为可切换模式。主调为墨黑、铜金、纸牌、青玉；月下符札区域可以使用更冷的月夜蓝紫，但权重低于茂一杀。

## 数值规范

- 字号只使用 `12 / 14 / 16 / 20 / 24 / 32 / 48px`。
- 字重只使用 `400 / 500 / 700`。
- 间距使用 8px 栅格，常用值为 `8 / 16 / 24 / 32 / 48px`。
- 圆角只使用 `4 / 8 / 12px`，按钮 8px，卡片 12px，小标签 4px。
- 阴影只使用单层阴影：默认 `0 2px 12px rgb(0 0 0 / 0.06)`，hover `0 4px 12px rgb(0 0 0 / 0.12)`。
- 大标题行高 1.2，小标题行高 1.4，正文长文本行高 1.7，短文本行高 1.6。

## 布局原则

- 首页首屏用身份说明加实机截图舞台。
- 茂一杀详情页按案例页写法组织：这是什么、玩家能玩到什么、怎么构建、优化记录、发布面板。
- 技术页用“技术负责什么”和“构建时间线”，不写成泛泛的建站教程。
- 下载页用发布面板，清楚区分完整包、累积更新包、SHA 和运行说明。

## 交互规则

- 全站 hover 使用 `cubic-bezier(0.33, 1, 0.68, 1)`，离场和主题切换使用 `cubic-bezier(0.4, 0, 0.2, 1)`。
- hover 时长 200ms，弹窗或导航状态变化 250ms，入场 300 到 400ms。
- 按钮 hover 使用 `translateY(-1px)`，active 使用 `scale(0.98)`。
- outline 按钮使用从左到右填充效果，文字链接使用从左到右下划线滑入。
- 导航滚动后从 64px 收敛到 56px，并启用半透明毛玻璃背景。
- 必须提供 `prefers-reduced-motion` 兜底。

## 图片规则

- 茂一杀只使用 `apps/site/public/media/maoyisha-*.webp` 及其生成的响应式版本。
- 月下符札只使用 `apps/site/public/media/yuexia-*.webp` 及其生成的响应式版本。
- 响应式图片生成到 `apps/site/public/media/generated`，宽度为 `480 / 960 / 1280`，同时生成极小模糊 placeholder。
- 禁止使用 `docs/ui-reference` 内的三国杀参考图作为成品素材。

## 内容规则

- 公开页面写给访客和玩家看，不出现站长待办语气。
- 不写“欢迎来到我的网站”“感谢访问”等空话。
- 下载区不能误导用户只下载单个 exe。
- 移动端 390px 和 430px 宽度下，中文长句、哈希和按钮文本不能溢出。
